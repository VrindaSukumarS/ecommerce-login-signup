<nav class="navbar navbar-expand-lg bg-dark bg-gradient">
  <div class="container-fluid">
    {{!-- <a class="navbar-brand fw-bold fs-4" href="#">Phobias Online Smartphones</a> --}}
      <a class="navbar-brand text-success fw-bold fs-3"><i class="fa-sharp fa-solid fa-book-open-reader"></i>Bookie</a>

      <div class="dropdown pe-5">
        <button class="btn btn-success dropdown-toggle" type="button" id="dropdownMenu2" data-bs-toggle="dropdown" aria-expanded="false">
          {{#if user}}
          {{user.name}}
          {{else}}
          Account
          {{/if}}
        </button>
        <ul class="dropdown-menu" aria-labelledby="dropdownMenu2">
          {{#if user}}
          <a href="/logout" style="text-decoration: none;"><li><button class="dropdown-item" type="button">Logout</button></li></a>
          {{else}}
          <a href="/login" style="text-decoration: none;"><li><button class="dropdown-item" type="button">Login</button></li></a>
          {{/if}}
        </ul>
      </div>

  </div>
</nav>